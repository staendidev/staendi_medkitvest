--site.staendi.dev--
Config = {}

Config.locale = 'de' --set locale ('en' for english or 'de' for german)

Config.itemRequiredVest = true --set if item is required for using vest

Config.itemRequiredMedikit = true --set if item is required for using medikit

Config.vestItem = 'vest' --set the item required for using the vest

Config.medkitItem = 'medikit' --set the item required for using the medkit

Config.cooldown = 10 --set the cooldown for reusing the vest / medikit

Config.defaultKeybindingVest = 'i' --set the default keybinding for using the vest

Config.defaultKeybindingMedkit = 'o' --set the default keybinding for using the medikit

--                         --
--   ENGLISH TRANSLATION   --
--                         --

Config.usedVestMessageEN = 'You used 1x bulletproof vest' --set the message on using the west

Config.usedMedikitMessageEN = 'You used 1x medkit' --set the message on using the medikit

Config.noVestMessageEN = 'You dont have any vests' --set the message when player has no vest item

Config.noMedikitMessageEN = 'You dont have any medkits' --set the message when player has no vest item

Config.keybindDescriptionVestEN = 'Use vest' --set the description of the vest keybind

Config.keybindDescriptionMedikitEN = 'Use medkit' --set the description of the medikit keybind

Config.cooldownActiveEN = 'Please wait a moment before using again' -- set the message when the cooldown ist active

--                         --
--   GERMAN TRANSLATION    --
--                         --

Config.usedVestMessageDE = 'Du hast 1x Weste verwendet' --set the message on using the west

Config.usedMedikitMessageDE = 'Du hast 1x Medikit verwendet' --set the message on using the medikit

Config.noVestMessageDE = 'Du hast keine Westen' --set the message when player has no vest item

Config.noMedikitMessageDE = 'Du hast keine Medikits' --set the message when player has no vest item

Config.keybindDescriptionVestDE = 'Weste verwenden' --set the description of the vest keybind

Config.keybindDescriptionMedikitDE = 'Medikit verwenden' --set the description of the medikit keybind

Config.cooldownActiveDE = 'Bitte warte einen Moment bis zum erneuten Verwenden' -- set the message when the cooldown ist active
